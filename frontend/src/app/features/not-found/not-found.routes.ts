import { NotFoundComponent } from './pages/not-found/not-found.component';
import type { Routes } from '@angular/router';

export const notFoundRoutes: Routes = [{ path: '', component: NotFoundComponent }];

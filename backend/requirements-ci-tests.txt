# Dependencies needed to run backend unit and integration tests in CI
boto3==1.43.18
# Keep this Pillow version in sync with backend/layers/image-processing-layer/requirements.txt
Pillow==12.2.0
pytest==9.0.3
